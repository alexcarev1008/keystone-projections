[project]
name = "keystone"
version = "0.1.0"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q -W ignore::DeprecationWarning -W ignore::FutureWarning"
